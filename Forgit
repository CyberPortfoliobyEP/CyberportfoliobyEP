sgpt --chat "Generate a Python script to perform ICMP scans for the subnet 10.10.1.0/24"

import os
for i in range(1, 255):
    response = os.system(f"ping -c 1 10.10.1.{i}")
    if response == 0:
        with open('icmp_results.txt', 'a') as f:
            f.write(f"10.10.1.{i} is reachable\n")

gh repo create network-scan-scripts --public
gh auth login
gh repo clone network-scan-scripts
mv icmp_scan.py network-scan-scripts/
cd network-scan-scripts
git add icmp_scan.py
git commit -m "Added ICMP scan script"
git push origin main

gh run script icmp_scan.py

10.10.1.11 is reachable
10.10.1.12 is reachable


git init
git remote add origin https://github.com/CyberPortfoliobyEP/AI.git
